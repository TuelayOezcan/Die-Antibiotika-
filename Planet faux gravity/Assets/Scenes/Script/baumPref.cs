using System.Collections;
using System.Collections.Generic;
using UnityEngine;
/*
// Zuweisung via ObjectInspector
public GameObject baumPref;


private void Start()
{
    // Temporäres Wandsegment
    GameObject tmpBaumPref;

    // Neues Wandsegment anlegen
    tmpBaumPref = (GameObject)Instantiate(baumPref, path.transform.position, path.transform.rotation);

    // Wandsegment scalieren
    tmpBaumPref.transform.localScale += new Vector3(x, y, z);

    // Wandsegment als Kind für diesen Raum hinzufügen
    tmpBaumPref.transform.parent = this.transform;
}

*/